import { TestContext } from '@salesforce/core/testSetup';

describe('demoutil tsp update', () => {
  const $$ = new TestContext();

  beforeEach(() => {});

  afterEach(() => {
    $$.restore();
  });
});
